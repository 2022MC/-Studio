# Start wrapper
HERE="$(dirname "$0")"
YT_DLP_BIN="$HERE/yt-dlp-bin"

if [ -f "$YT_DLP_BIN" ]; then
    "$YT_DLP_BIN" --no-warnings "$@"
else
    echo "ERROR: Local yt-dlp binary not found!" >&2
    exit 1
fi
